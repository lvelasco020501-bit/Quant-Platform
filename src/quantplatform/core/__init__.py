"""Domain core: models, ports, enums, errors and pure utilities.

This package must not import from any other platform package. That rule is verified by the
dependency boundary tests and is what keeps the domain independent of venues, storage and
delivery mechanisms.
"""
